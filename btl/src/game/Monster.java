package game;

import java.awt.*;

public class Monster extends Character{
    State state;
    public Monster(int x, int y, int size, Image image) {
        super(x, y, size, image);
        state = new State(//abc);
    }

}
